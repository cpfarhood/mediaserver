apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: transmission-movies
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  serviceName: transmission-movies
  selector:
    matchLabels:
      app.kubernetes.io/name: transmission-movies
  template:
    metadata:
      labels:
        app.kubernetes.io/name: transmission-movies
    spec:
      initContainers:
      - name: copy-openvpn-config
        image: busybox:1.36
        command: ["/bin/sh", "-c", "cp /tmp/openvpn/us-il-05.protonvpn.udp.ovpn /etc/openvpn/custom/"]
        volumeMounts:
        - name: openvpn-config
          mountPath: /tmp/openvpn
        - name: openvpn-custom
          mountPath: /etc/openvpn/custom
      containers:
      - name: transmission
        image: haugene/transmission-openvpn:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 9091
          name: http-9091
        envFrom:
        - configMapRef:
            name: transmission-movies-config
        - secretRef:
            name: openvpn-credentials
        - secretRef:
            name: transmission-credentials
        resources:
          requests:
            cpu: 250m
            memory: 1Gi
          limits:
            memory: 8Gi
        securityContext:
          privileged: true
          capabilities:
            add:
              - NET_ADMIN
        volumeMounts:
        - name: config
          mountPath: "/config"
        - name: openvpn-custom
          mountPath: /etc/openvpn/custom
        - name: movies0
          mountPath: /media/movies0
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      volumes:
      - name: openvpn-config
        configMap:
          name: transmission-movies-config
          items:
          - key: us-il-05.protonvpn.udp.ovpn
            path: us-il-05.protonvpn.udp.ovpn
      - name: openvpn-custom
        emptyDir: {}
      - name: movies0
        persistentVolumeClaim:
          claimName: movies0
  volumeClaimTemplates:
  - metadata:
      name: config
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 4Gi

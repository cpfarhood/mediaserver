apiVersion: v1
kind: Service
metadata:
  name: transmission-movies
  annotations:
    service.cilium.io/forwarding-mode: dsr
    external-dns.alpha.kubernetes.io/hostname: "transmission-movies.animaniacs.farh.net"
spec:
  selector:
    app.kubernetes.io/name:  transmission-movies
  ports:
  - port: 9091
  type: LoadBalancer
  externalTrafficPolicy: Local